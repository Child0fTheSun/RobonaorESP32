#pragma once

#include "kinematics.h" // JointAngles, DOF

// =============================================================================
// Интерполятор траектории в пространстве обобщённых координат
//
// Метод: натуральный кубический сплайн (natural cubic spline).
//
// Использование:
//   1. Добавить опорные точки: addWaypoint(t, q)  (t в секундах)
//   2. Построить сплайн:       build()
//   3. Вычислить угла суставов в момент t: evaluate(t, q_out)
//
// Ограничения:
//   - Минимум 2, максимум 5 опорных точек
//   - Времена точек должны быть строго возрастающими
// =============================================================================

class TrajectoryInterpolator {
public:
    static constexpr size_t MAX_WAYPOINTS = 5;
    static constexpr size_t MIN_WAYPOINTS = 2;

    TrajectoryInterpolator();

    // -------------------------------------------------------------------------
    // Добавить опорную точку.
    //   t — время (с), должно быть строго больше времени предыдущей точки.
    //   q — углы суставов (рад).
    // Возвращает false, если достигнут MAX_WAYPOINTS или t некорректно.
    // -------------------------------------------------------------------------
    bool addWaypoint(float t, const JointAngles& q);

    // Сбросить все точки и пересчитанные коэффициенты.
    void clear();

    // Текущее количество добавленных точек.
    size_t getWaypointCount() const { return m_count; }

    // -------------------------------------------------------------------------
    // Построить кубический сплайн (вызывать после добавления >= 2 точек).
    // Возвращает false, если точек недостаточно.
    // -------------------------------------------------------------------------
    bool build();

    // Сплайн построен?
    bool isBuilt() const { return m_built; }

    // -------------------------------------------------------------------------
    // Вычислить углы суставов в момент времени t.
    //   - При t < t_start  возвращает начальную точку (без ошибки).
    //   - При t > t_end    возвращает конечную точку (без ошибки).
    // Возвращает false, если сплайн не построен.
    // -------------------------------------------------------------------------
    bool evaluate(float t, JointAngles& q_out) const;

    // Полная длительность траектории (с). 0, если < 2 точек.
    float getDuration() const;

    // Время первой и последней точек.
    float getStartTime() const { return (m_count > 0) ? m_times[0] : 0.0f; }
    float getEndTime()   const { return (m_count > 1) ? m_times[m_count - 1] : 0.0f; }

private:
    // Опорные точки
    float       m_times[MAX_WAYPOINTS];
    JointAngles m_points[MAX_WAYPOINTS];
    size_t      m_count;
    bool        m_built;

    // Коэффициенты кубического полинома на каждом сегменте [t_i, t_{i+1}]:
    //   q_j(dt) = A[i][j] + B[i][j]*dt + C[i][j]*dt^2 + D[i][j]*dt^3
    //   где dt = t - t_i
    float m_A[MAX_WAYPOINTS - 1][DOF];
    float m_B[MAX_WAYPOINTS - 1][DOF];
    float m_C[MAX_WAYPOINTS - 1][DOF];
    float m_D[MAX_WAYPOINTS - 1][DOF];

    // Построить сплайн для одного сустава j.
    void buildJoint(size_t j);
};
